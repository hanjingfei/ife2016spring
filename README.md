# ife2016spring
```
//initial
git branch gh-pages
git push -u origin gh-pages

//merge
git checkout gh-pages
git merge master
git push origin gh-pages

//view
https://hanjingfei.github.io/ife2016spring/
```
